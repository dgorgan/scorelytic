{
  "name": "@scorelytic/scripts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "echo 'No dev server for scripts'",
    "build": "tsc -p tsconfig.json",
    "format": "turbo run format --filter=scripts...",
    "type-check": "tsc --noEmit",
    "test": "turbo run test --filter=scripts..."
  },
  "dependencies": {
    "@scorelytic/server": "workspace:*",
    "@scorelytic/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
