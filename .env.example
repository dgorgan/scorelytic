# === General Configuration ===
PORT=5000

# === Supabase ===
SUPABASE_URL=http://localhost:54321
SUPABASE_SERVICE_ROLE_KEY=dummy
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=dummy

# === OpenAI / LLM (Server) ===
OPENAI_API_KEY=dummy
DISABLE_OPENAI=false
LLM_PROMPT_STYLE=DEFAULT

# === API (Server-side) ===
NEXT_PUBLIC_API_URL=http://localhost:5000

# === Logging / Monitoring (Sentry) ===
# Server Sentry Settings
SENTRY_DSN_SERVER=dummy
SENTRY_PROJECT_SERVER=node-express
SENTRY_RELEASE=local
SENTRY_ENVIRONMENT=development

# Client Sentry Settings
NEXT_PUBLIC_SENTRY_DSN=dummy
NEXT_PUBLIC_SENTRY_ENVIRONMENT=development
NEXT_PUBLIC_SENTRY_RELEASE=local

# Shared Sentry Settings (for both server and client)
SENTRY_ORG=scorelytic
SENTRY_AUTH_TOKEN=dummy

# === Client-Specific Variables ===
NEXT_PUBLIC_SENTRY_PROJECT_CLIENT=javascript-nextjs
TURBO_TEAM='scorelytic-client'

# === Shared / Other ===
YOUTUBE_API_KEY=dummy

# === Environment-specific (for Production/Staging) ===
# Set these dynamically in CI or production/staging environments
# e.g., github.sha for SENTRY_RELEASE in CI pipelines
SENTRY_AUTH_TOKEN=
SENTRY_ENVIRONMENT=
SENTRY_ORG=
SENTRY_PROJECT_CLIENT=
SENTRY_DSN_CLIENT=
SENTRY_PROJECT_SERVER=
SENTRY_DSN_SERVER=
